# Program 4
    irmovq $10, %rax         # irmovq: Load immediate 10 into %rax
    irmovq $5, %rbx          # Load immediate 5 into %rbx

    addq %rbx, %rax          # OPq: Add %rbx to %rax
    subq %rbx, %rax          # Subtract %rbx from %rax
    andq %rbx, %rax          # AND %rbx with %rax
    xorq %rbx, %rax          # XOR %rbx with %rax

    rrmovq %rax, %rcx        # rrmovq: Move %rax to %rcx
    cmovg %rbx, %rcx         # cmovXX: Move %rbx to %rcx if greater

    jmp end                 # jXX: Unconditional jump to label .end
end
    halt                     # halt: Stop the program
